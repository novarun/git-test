pipeline {
    agent any

    stages {
        stage('Create Directory') {
            steps {
                sh('mkdir JenkinsAssignment1')
            }
        }
        stage('Enter Directory') {
            steps {
                sh('cd JenkinsAssignment1')
            }
        }
        stage('Git Init') {
            steps {
                sh('git init')
            }
        }
        stage('Git Pull') {
            steps {
                //git pull credentialsId: 'novarun', url: 'https://github.com/novarun/GitAssignments5.git'
                sh('git pull https://github.com/novarun/GitAssignments5.git')
            }
        }
    }
}
